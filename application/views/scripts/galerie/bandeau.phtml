<div class="totalBox">
    <?php if ($this->imgToCrop !== null): ?>
        <div class="box">
            <h2 class="titreNews">Mise en forme de l'image</h2>
            <form method="POST" action="<?= $this->url(array('controller' => 'galerie', 'action' => 'bandeau')) ?>">
                <img src="<?= $this->imgToCrop; ?>" id="crop" name="crop" />
                <input type="hidden" id="x1" name="x1" />
                <input type="hidden" id="y1" name="y1" />
                <input type="hidden" id="width" name="width" />
                <input type="hidden" id="height" name="height" />
                <input type="hidden" name="imgCrop" value="<?= $this->imgToCrop; ?>" />
                <input class="bouton1" type="submit" value="Envoyer" />
            </form>
        </div>
    <?php endif; ?>
    <div class="box">
        <h1 class="titreNews">Liste des images</h1>
        <ul>
            <?php foreach ($this->images as $image): ?>
                <li class="lstImages">
                    <img src="<?= $image->getNomWithPath(); ?>" />
                    <a href="<?= $this->url(array('controller' => 'galerie', 'action' => 'supprimerBandeau', 'id' => $image->getId())); ?>"
                       class="bouton1">
                        Supprimer
                    </a>
                </li>
            <?php endforeach; ?>
        </ul>
    </div>
    <div class="box">
        <h2 class="titreNews">Ajouter une image</h2>
        <form method="POST"
              action="<?= $this->url(array('controller' => 'galerie', 'action' => 'ajouterBandeau')); ?>" 
              id="frmAdd"
              enctype="multipart/form-data">
            <div><input type="file" id="imgSrc" name="imgSrc" class="required" /></div>
            <ul>
                <?php foreach($this->errorMessages as $message): ?>
                <li class="error"><?= $message; ?></li>
                <?php endforeach; ?>
            </ul>
            <input type="submit" value="Envoyer" class="bouton1" />
        </form>
    </div>
</div>