<div id="globalContent">
    <!-- div du menu -->
    <?= $this->partial('_sidebar.phtml', array('sections' => $this->sections, 'id' => $this->club->id, 'controller' => $this->controller)) ?>
    <!-- div du contenu de la page -->
    <div class="box" id="news">
        <div class="titreNews"><span><?= $this->escape($this->club->titre); ?></span></div>
        <?php if ($this->club->id == 3): //Section l'équipe => Trombinoscope ?>
            <?= $this->partial('_trombi.phtml', array('everybody' => $this->everybody, 'nbPerLine' => 4)); ?>
        <?php else: ?>
            <div class="contenuNews">
                <div><?= $this->club->contenu ?></div>
            </div>
            <?php if ($this->club->id == 5): // Pour les lieux d'entrainement ?>
                <?php foreach ($this->lieux as $lieu): ?>
                    <div class="box map" id="map<?= $lieu->numero; ?>">
                        <div class="titreNews"><?= $this->escape($lieu->nom); ?></div>
                        <div id="map_canvas<?= $lieu->numero; ?>" class="canvas"></div>
                        <script type="text/javascript">
                            $(document).ready(function() {
                                $('#map_canvas<?= $lieu->numero; ?>').gmap3({
                                    action: 'addMarker',
                                    address: "<?= $this->escape($lieu->adresse); ?>",
                                    map:{
                                        center: true,
                                        zoom: 14
                                    }
                                });
                            });
                        </script>
                        <p><?= $this->escape($lieu->adresse); ?></p>
                    </div>
                <?php endforeach; ?>
            <?php endif; ?>
        <?php endif; ?>
    </div>
</div>
<?php //Javascript chelou pour l'admin ?>
