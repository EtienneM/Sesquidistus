<div id="Menu" class="box">
    <div class="titreNews">Sections</div>
    <?php foreach ($this->sections as $section): ?>
        <a href="<?= $this->url(array('controller' => $this->controller, 'action' => 'index', 'id' => $section->id)) ?>">
            <?php $class = ($section->id == $this->id) ? 'bouton6' : 'bouton2' ?>
            <div class="<?= $class ?> bouton">
                <span><?= $this->escape($section->titre) ?></span>
            </div>
        </a>
    <?php endforeach; ?>
    <?php if ($this->isAdmin()): ?>
        <span class="bouton1 actionContent" id="ajoutCat" title="Ajouter une catégorie">+</span>
        <span class="bouton1 actionContent" id="supprCat" title="Supprimer une catégorie">-</span>
        <a class="doc" href="/doc/doc_club_ultimate/doc_admin_club_ultimate.html" target="_blank">
            <img src="/images/help.png" title="aide en ligne" alt="aide"/>
        </a>
        <div id="addCategory" style="display:none;">
            <p class="i">Veuillez entrer le nom de la nouvelle catégorie&nbsp;:</p>
            <form id="formAjoutCat" method="post" action="<?= $this->url(array('controller' => $this->controller, 'action' => 'ajouter')); ?>">
                <input type="text" size=30 name="addTitle" id="addTitle" />
            </form>
        </div>
        <div id="delCategory" style="display:none;">
            <p class="i">Veuillez sélectionner la catégorie à supprimer&nbsp;:</p>
            <form id="formSupprCat" method="post" action="<?= $this->url(array('controller' => $this->controller, 'action' => 'supprimer')); ?>">
                <select id="delCat" name="delCat">
                    <?php
                    foreach ($this->sections as $section):
                        if ($section->id == 3 && $this->controller == 'club') {
                            continue;
                        }
                        ?>
                        <option value="<?= $section->id ?>"><?= $this->escape($section->titre) ?></option>
                    <?php endforeach; ?>
                </select>
            </form>
        </div>
<?php endif; ?>
</div>

